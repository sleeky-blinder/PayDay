/* Payday — sync configuration.
   Paste your Supabase project URL and anon (public) key here, or leave both empty
   and enter them later under Settings → Backup → Sync connection.
   The anon key is safe to publish: row-level security keeps your data yours. */
window.PAYDAY_CONFIG = {
  url: "",
  key: ""
};
